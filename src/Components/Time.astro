---
const date = new Date()
const hours = String(date.getHours()).padStart(2, '0')
const minutes = String(date.getMinutes()).padStart(2, '0')
const formattedTime = `${hours}:${minutes}`
---

<p class='text-textColor-primary font-bold text-5xl'>{formattedTime}</p>
